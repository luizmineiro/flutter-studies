import 'package:flutter/material.dart';

class AddToBuyItemScreen extends StatelessWidget {
  const AddToBuyItemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
